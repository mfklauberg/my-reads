const NONE = { display: 'none', key: 'none', color: '' };
const READ = { display: 'Read', key: 'read', color: '3FB763' };
const WANT_TO_READ = { display: 'Want to Read', key: 'wantToRead', color: '008EE8' };
const CURRENTLY_READING = { display: 'Currently Reading', key: 'currentlyReading', color: 'FFDB1F' };

export default {
  CURRENTLY_READING,
  WANT_TO_READ,
  READ,
  NONE,
};

export {
  NONE,
  READ,
  WANT_TO_READ,
  CURRENTLY_READING,
};